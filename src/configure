#! /bin/sh
# run the automatically generated configure script
mkdir -p auto

autoconf -o auto/configure && CONFIG_STATUS=auto/config.status auto/configure "$@" --srcdir="${srcdir:-.}" --cache-file=auto/config.cache

rm -rf autom4te.cache

result=$?

exit $result
